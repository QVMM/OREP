# 小启 · 借鉴 Grok Build 的落地清单（不搬编码 Agent）

原则：只借 **上下文工程 / 任务包 / 两阶段确认 / 工具优先** 等思路；不引入改仓库、终端沙箱、coding 工具环。

---

## P0（本迭代：Skills 体系）

| 项 | 说明 | 状态 |
|---|---|---|
| Skills 发现与加载 | `skills/catalog/*/SKILL.md`，YAML frontmatter | 本迭代 |
| 匹配 | slash（`/开场`）+ 关键词触发 | 本迭代 |
| 注入 | 命中 skill 写入 system，限 1～2 个、控长度 | 本迭代 |
| 可观测 | SSE `skill_match` / step「启用技能」 | 本迭代 |
| 核心包 | 开场 / 评分复盘 / 今日安排 / 讲稿改写 | 本迭代 |

---

## P1（下一迭代）

| 项 | 说明 | 状态 |
|---|---|---|
| 前端 slash 菜单 | 输入 `/` 弹出技能列表；欢迎区 skill chips；工具栏 `/` 按钮 | **已完成**（工作台 + 首页半栏） |
| 工具优先强化 | skill 声明 `need_scores/...`，强制开工具门闩 | P0 已做核心 |
| Plan 轻确认 | 「本周计划 / 大改讲稿」先出草案卡，用户确认再展开全文 | **已完成**（SSE plan_draft + 前端卡） |
| 引用强化 | 回答必须带「依据：训练营/评分/资料」脚注 | **已完成**（citation + 强制依据 + 文末补全） |
| 赛道 RULES + 匿名规范包 | `tracks/*/RULES.md` + `_common/ANONYMITY.md` 注入 | **已完成**（餐饮 / 新一代信息技术 样例） |

---

## P2（中期）

| 项 | 说明 | 状态 |
|---|---|---|
| 轻量 Memory | 跨会话：赛道、项目名、开场偏好（非 coding memory） | 待做（现有「写作习惯」面板为旁路） |
| 子任务 worker | 只读：查分 / 查训练 / 查协作 并行，主脑综合 | 待做 |
| 校方/赛道规则 | 类似 AGENTS.md：`tracks/<id>/RULES.md` 注入 | 与 P1 规范包合并规划 |
| Hooks | 回答后：敏感词/假数据扫描；生成文件前确认 | 待做 |
| 个人云盘 vs 资源中心 | **不做个人空间**，保留资源中心 | 已取消 |

---

## 明确不做

- 文件编辑工具、apply_patch、终端 sandbox  
- 通用 coding subagent  
- 复刻 Grok TUI
