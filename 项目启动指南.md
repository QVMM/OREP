# OREP 在线路演评估平台 — 项目启动指南

> 适用环境：macOS (Apple Silicon)，本地开发环境  
> 最后更新：2026-03-23

---

## 目录结构

```
~/项目/OREP/
├── backend/                        # Spring Boot 后端
│   ├── src/main/java/com/orep/backend/
│   │   ├── controller/             # REST 控制器
│   │   ├── service/                # 业务逻辑
│   │   ├── mapper/                 # MyBatis-Plus Mapper
│   │   ├── entity/                 # 实体类
│   │   ├── config/                 # 配置类（JwtUtil 等）
│   │   └── common/                 # 通用工具（Result 等）
│   ├── src/main/resources/
│   │   ├── application.yml         # 后端配置文件
│   │   ├── static/ppt-templates/   # PPT 静态资源
│   │   └── sql/                    # 建表 SQL
│   ├── uploads/                    # 用户上传文件存储
│   ├── pom.xml                     # Maven 依赖
│   └── target/                     # 编译输出
│
├── frontend/
│   ├── user/                       # 用户前端 (Vue 3 + Element Plus)
│   │   ├── src/views/              # 页面组件
│   │   ├── src/router/index.js     # 前端路由
│   │   ├── src/utils/request.js    # Axios 封装
│   │   └── vite.config.js          # Vite 配置 + 代理
│   └── admin/                      # 管理后台 (Vue 3 + Element Plus)
│       ├── src/views/
│       ├── src/router/index.js
│       └── vite.config.js
│
├── signaling/                      # 信令服务 (Node.js + Socket.IO)
│   ├── server.js                   # 主入口
│   └── package.json
│
├── certs/                          # 自签证书
│   ├── cert.pem
│   └── key.pem
│
├── livekit.yaml                    # LiveKit Server 配置
├── livekit-proxy.js                # LiveKit WebSocket 代理（/rtc/v1 → /rtc 路径转换）
├── sql/                            # 数据库初始化 SQL
├── deploy/                         # 部署相关配置
└── README.md
```

---

## 依赖环境

| 组件 | 版本 | 路径 |
|------|------|------|
| Java | JDK 21 | `/Library/Java/JavaVirtualMachines/jdk-21.jdk/Contents/Home/` |
| Maven | 3.9.14 | `/opt/homebrew/var/homebrew/tmp/.cellar/maven/3.9.14/libexec/` |
| Node.js | v25+ | 系统安装 |
| LiveKit Server | 1.9.12 | `livekit-server`（PATH 中） |
| MySQL | 8.x | `10.199.12.13:3306` |
| Redis | 7.x | `127.0.0.1:6379` |

---

## 一键启动所有服务

```bash
cd ~/项目/OREP

# 1. 停止所有残留进程
kill -9 $(ps aux | grep -E "spring-boot|OrepApplication|vite|livekit|livekit-proxy|node.*server" | grep -v grep | awk '{print $2}') 2>/dev/null

# 2. 启动后端
export JAVA_HOME=$(/usr/libexec/java_home -v 21)
export M2_HOME=/opt/homebrew/var/homebrew/tmp/.cellar/maven/3.9.14/libexec
cd ~/项目/OREP/backend && nohup $M2_HOME/bin/mvn spring-boot:run -q > /tmp/orep-backend.log 2>&1 &

# 3. 启动 LiveKit Server
cd ~/项目/OREP && nohup livekit-server --config livekit.yaml > /tmp/orep-livekit.log 2>&1 &

# 4. 启动 LiveKit Proxy（路径转换）
cd ~/项目/OREP && nohup node livekit-proxy.js > /tmp/orep-livekit-proxy.log 2>&1 &

# 5. 启动信令服务
cd ~/项目/OREP/signaling && nohup node server.js > /tmp/orep-signaling.log 2>&1 &

# 6. 启动用户前端
cd ~/项目/OREP/frontend/user && nohup npx vite --port 5174 > /tmp/orep-frontend.log 2>&1 &

# 7. 启动管理前端
cd ~/项目/OREP/frontend/admin && nohup npx vite --port 5173 > /tmp/orep-admin.log 2>&1 &

echo "等待启动..."; sleep 10
echo "完成"
```

---

## 分项启动 / 停止命令

### 1. 后端 (Spring Boot)

```bash
# 启动
export JAVA_HOME=$(/usr/libexec/java_home -v 21)
export M2_HOME=/opt/homebrew/var/homebrew/tmp/.cellar/maven/3.9.14/libexec
cd ~/项目/OREP/backend && nohup $M2_HOME/bin/mvn spring-boot:run -q > /tmp/orep-backend.log 2>&1 &

# 编译（不重启）
cd ~/项目/OREP/backend && $M2_HOME/bin/mvn compile -q

# 停止
kill -9 $(ps aux | grep "OrepApplication\|spring-boot:run" | grep -v grep | awk '{print $2}') 2>/dev/null

# 查看日志
tail -f /tmp/orep-backend.log

# 端口: 8080
# 配置: backend/src/main/resources/application.yml
```

### 2. LiveKit Server

```bash
# 启动
cd ~/项目/OREP && nohup livekit-server --config livekit.yaml > /tmp/orep-livekit.log 2>&1 &

# 停止
kill -9 $(ps aux | grep "livekit-server" | grep -v grep | awk '{print $2}') 2>/dev/null

# 查看日志
tail -f /tmp/orep-livekit.log

# 端口: 7880 (HTTP), 7881 (TCP), 50100-50200 (UDP)
# 配置: livekit.yaml
```

### 3. LiveKit Proxy

```bash
# 启动
cd ~/项目/OREP && nohup node livekit-proxy.js > /tmp/orep-livekit-proxy.log 2>&1 &

# 停止
kill -9 $(ps aux | grep "livekit-proxy" | grep -v grep | awk '{print $2}') 2>/dev/null

# 端口: 7882 (HTTPS)
# 作用: 将 LiveKit Client 2.x 的 /rtc/v1 路径转换为 Server 1.9.12 的 /rtc
```

### 4. 信令服务 (Socket.IO)

```bash
# 启动
cd ~/项目/OREP/signaling && nohup node server.js > /tmp/orep-signaling.log 2>&1 &

# 停止
kill -9 $(ps aux | grep "node.*server" | grep -v grep | awk '{print $2}') 2>/dev/null

# 查看日志
tail -f /tmp/orep-signaling.log

# 端口: 3000 (HTTPS)
```

### 5. 用户前端 (Vite Dev Server)

```bash
# 启动
cd ~/项目/OREP/frontend/user && nohup npx vite --port 5174 > /tmp/orep-frontend.log 2>&1 &

# 停止
kill -9 $(ps aux | grep "vite.*5174" | grep -v grep | awk '{print $2}') 2>/dev/null

# 查看日志
tail -f /tmp/orep-frontend.log

# 端口: 5174 (HTTPS)
# 配置: frontend/user/vite.config.js
```

### 6. 管理前端 (Vite Dev Server)

```bash
# 启动
cd ~/项目/OREP/frontend/admin && nohup npx vite --port 5173 > /tmp/orep-admin.log 2>&1 &

# 停止
kill -9 $(ps aux | grep "vite.*5173" | grep -v grep | awk '{print $2}') 2>/dev/null

# 查看日志
tail -f /tmp/orep-admin.log

# 端口: 5173 (HTTPS)
# 配置: frontend/admin/vite.config.js
```

---

## 访问地址

| 服务 | 本机 | 局域网 |
|------|------|--------|
| 用户前端 | https://localhost:5174 | https://10.199.12.13:5174 |
| 管理后台 | https://localhost:5173 | https://10.199.12.13:5173 |
| 后端 API | http://localhost:8080 | http://10.199.12.13:8080 |
| LiveKit | http://localhost:7880 | http://10.199.12.13:7880 |
| LiveKit Proxy | https://localhost:7882 | https://10.199.12.13:7882 |
| 信令服务 | https://localhost:3000 | https://10.199.12.13:3000 |

> **管理员登录**: `admin` / `admin123`  
> **HTTPS 证书**: 自签证书，浏览器会提示不安全，点「继续访问」即可

---

## 停止所有服务

```bash
kill -9 $(ps aux | grep -E "spring-boot|OrepApplication|livekit-server|livekit-proxy|node.*server|vite" | grep -v grep | awk '{print $2}') 2>/dev/null
echo "已停止所有服务"
```

---

## 检查服务状态

```bash
# 检查进程
ps aux | grep -E "spring-boot|livekit|vite|node.*server" | grep -v grep | awk '{printf "%-6s %s\n", $2, $NF}'

# 检查端口
for port in 8080 5173 5174 3000 7880 7882; do
  if curl -sk -o /dev/null -w "%{http_code}" "http://localhost:$port/" 2>/dev/null | grep -qE "200|302|404"; then
    echo "✓ :$port (HTTP)"
  elif curl -sk -o /dev/null -w "%{http_code}" "https://localhost:$port/" 2>/dev/null | grep -qE "200|302|404"; then
    echo "✓ :$port (HTTPS)"
  else
    echo "✗ :$port 未响应"
  fi
done

# 检查后端 API
curl -s http://localhost:8080/api/auth/login -X POST -H 'Content-Type: application/json' \
  -d '{"username":"admin","password":"admin123"}' | python3 -c \
  "import sys,json; d=json.load(sys.stdin); print('✓ 后端正常' if d['code']==200 else '✗ 后端异常')"
```

---

## 关键配置文件

### `backend/src/main/resources/application.yml`

```yaml
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://10.199.12.13:3306/orep?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 12345678
  data:
    redis:
      host: 127.0.0.1      # Redis 本机
      port: 6379

livekit:
  ws-url: ws://10.199.12.13:7880

file:
  upload-dir: /Users/liuyixing/项目/OREP/backend/uploads
```

### `livekit.yaml`

```yaml
port: 7880
bind_addresses:
  - "0.0.0.0"
rtc:
  tcp_port: 7881
  port_range_start: 50100
  port_range_end: 50200
  use_external_ip: false
keys:
  devkey-orep-local: OREP2026LocalDevSecretKey32Characters
```

### Vite 代理规则（两个前端通用）

| 路径 | 代理目标 | 说明 |
|------|----------|------|
| `/api` | `http://127.0.0.1:8080` | 后端 API |
| `/uploads` | `http://127.0.0.1:8080` | 文件上传/下载 |
| `/ws` | `http://127.0.0.1:8080` | WebSocket |
| `/socket.io` | `https://127.0.0.1:3000` | 信令服务 |
| `/livekit` | `http://127.0.0.1:7880` | LiveKit（路径重写 `/rtc/v1` → `/rtc`） |

---

## 常见问题

### LiveKit WebSocket 连接失败 `v1 RTC path not found`
- **原因**: LiveKit Client 2.x 用 `/rtc/v1/`，Server 1.9.12 用 `/rtc/`
- **解决**: 必须启动 LiveKit proxy（端口 7882），前端连接 `wss://host:7882`

### 后端编译错误 `不兼容的类型: 从double转换到float`
- **原因**: Java float 数组字面量需加 `f` 后缀，如 `1.5f` 而非 `1.5`

### 前端 `vite.config.js` 解析错误
- **检查**: 是否有被 IP 替换脚本误改的内容（如 `true` → `tr10.199.12.13`）
- **解决**: 重新写入正确的 vite.config.js

### `LiveKitController` 404
- **原因**: Java 文件缺少 `package com.orep.backend.controller;` 声明
- **解决**: 文件首行添加 package 声明，重新编译

### Vue Router `next()` 警告
- **原因**: Vue Router 4 已弃用 `next()` 回调
- **解决**: 改为 `router.beforeEach((to, from) => { return '/path' })`

### 外部电脑无法访问
- **检查**: 所有服务是否绑定 `0.0.0.0`（Vite 需 `host: '0.0.0.0'`）
- **检查**: macOS 防火墙是否放行端口
- **必须开放端口**: 5173, 5174, 8080, 3000, 7880, 7882

### MySQL 不可达
- 确认 `10.199.12.13:3306` 可访问
- 确认 `application.yml` 中的连接信息正确

---

## 后端编译（仅编译不运行）

```bash
export JAVA_HOME=$(/usr/libexec/java_home -v 21)
export M2_HOME=/opt/homebrew/var/homebrew/tmp/.cellar/maven/3.9.14/libexec
cd ~/项目/OREP/backend && $M2_HOME/bin/mvn compile -q
```

## 前端安装依赖（首次或更新后）

```bash
cd ~/项目/OREP/frontend/user && npm install
cd ~/项目/OREP/frontend/admin && npm install
cd ~/项目/OREP/signaling && npm install
```
