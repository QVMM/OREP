# OREP 音视频融合能力画像 — 开发路线规划

## 📌 核心目标

将路演评估从"纯音频评分"升级为"音视频融合的全方位能力画像"：
- **五维一体评分**：内容质量、表达力、逻辑结构、技术深度、团队协作
- **时间序列融合**：音频特征（语速/停顿/填充词）+ 视觉特征（肢体/表情/眼神）按时间窗口对齐
- **能力画像**：LLM 生成描述性选手画像，不做分数裁判，做证据分析师

---

## 🏗 现有架构

```
会议录制（前端）
├── 音频录制 ✅ AudioRecorder.vue → webm 上传
└── 视频录制 ❌ 未实现（但 LiveKit 已有 video track）

AI评分服务（Python FastAPI）
├── ASR 语音识别 ✅ asr_service.py (fun-asr-realtime)
├── 语音质量分析 ✅ speech_analysis_service.py (语速/停顿/填充词/语调)
├── LLM内容评分 ✅ llm_scoring_service.py (DeepSeek/MiniMax，5维度)
├── PDF报告生成 ✅ report_service.py
├── 流水线编排 ✅ pipeline_service.py
└── 视频分析 ❌ 未实现

后端（Spring Boot）
├── 会议管理 ✅
├── 音频上传 API ✅ (via Python AI service)
└── 评分结果查询 ✅

前端（Vue.js）
├── 会议室 ✅ MeetingRoom.vue
├── 评分结果展示 ✅ ScoreResult.vue
└── 视频分析展示 ❌ 未实现
```

---

## 🚀 开发计划（4个阶段）

### 阶段一：视频录制（1-2天）

**目标**：会议室能同时录制音频+视频，作为 AI 分析的数据源

#### 1.1 前端：AudioRecorder.vue 升级为 MediaRecorder

当前 `AudioRecorder.vue` 只录制音频流。需要升级为同时录制视频。

**改动点**：
- `AudioRecorder.vue` → 改名为 `MediaRecorder.vue`（或保持文件名，扩展功能）
- 录制时获取屏幕共享的 video track（MeetingRoom 已有 `getDisplayMedia`）
- 合并 audio + video tracks 为一个 MediaStream 录制
- 输出格式：webm（含音视频轨）
- 上传时标记 `has_video=true`

**关键代码逻辑**：
```javascript
// 获取已有的音频流
const audioStream = audioContext.createMediaStreamDestination().stream
// 获取屏幕共享的视频流（MeetingRoom 已有）
const videoStream = screenShareStream
// 合并
const combined = new MediaStream([
  ...audioStream.getAudioTracks(),
  ...videoStream.getVideoTracks()
])
const recorder = new MediaRecorder(combined, {
  mimeType: 'video/webm;codecs=vp8,opus'
})
```

**文件改动**：
- `frontend/user/src/components/AudioRecorder.vue` — 增加视频录制
- `frontend/user/src/views/MeetingRoom.vue` — 传递 video track 给 recorder

#### 1.2 后端：接收视频文件

当前 `ai-scoring/app/routers/scoring_router.py` 的 `upload-audio` 接口只接收音频。
需要：
- 扩展允许的文件格式：增加 `.webm`（音视频）、`.mp4`
- 保存时区分音频文件和视频文件
- pipeline 启动时自动从视频中提取音频

**文件改动**：
- `ai-scoring/app/routers/scoring_router.py` — 扩展上传接口
- `ai-scoring/app/services/audio_service.py` — 增加视频→音频提取

---

### 阶段二：视频分析模块（2-3天）

**目标**：新增视频分析服务，复用已验证的 Qwen3-VL-Flash 方案

#### 2.1 新增 `video_analysis_service.py`

```
ai-scoring/app/services/
├── asr_service.py          ✅ 保留
├── speech_analysis_service.py  ✅ 保留
├── llm_scoring_service.py  ✅ 保留
├── video_analysis_service.py   🆕 新增
├── fusion_service.py       🆕 新增
├── pipeline_service.py     🔄 改造
└── report_service.py       🔄 改造
```

**video_analysis_service.py 核心功能**：
```python
def extract_keyframes(video_path: str, interval_sec: int = 120) -> list[str]:
    """从视频中按固定间隔抽取关键帧"""
    # ffmpeg -i video.mp4 -vf "fps=1/120" frames/frame_%03d.jpg

def analyze_frames(frame_paths: list[str], batch_size: int = 5) -> list[dict]:
    """用 Qwen3-VL-Flash 批量分析帧"""
    # 每批5帧发送给多模态模型
    # 返回每帧的：肢体语言、姿态、表情、眼神、场景

def run_video_analysis(video_path: str, interval_sec: int = 120) -> dict:
    """完整视频分析流水线"""
    frames = extract_keyframes(video_path, interval_sec)
    results = analyze_frames(frames)
    return {
        "frame_count": len(results),
        "interval_sec": interval_sec,
        "per_frame": results,
        "aggregates": compute_aggregates(results)
    }
```

**配置项**（加入 `config.py`）：
- `VIDEO_FRAME_INTERVAL` = 120（每2分钟抽一帧）
- `VIDEO_BATCH_SIZE` = 5（每批5帧）
- `VIDEO_MODEL` = "qwen3-vl-flash"

#### 2.2 新增 `fusion_service.py`

**核心功能**：
```python
def fuse_audio_video(
    audio_features: list[dict],  # 每窗口音频特征（语速/停顿/填充词）
    video_features: list[dict],  # 每帧视觉特征（肢体/表情/眼神）
    window_sec: int = 120
) -> list[dict]:
    """按时间窗口对齐音视频数据"""

def compute_fusion_score(window: dict) -> float:
    """计算单窗口融合评分"""
    # 视觉综合 * 0.4 + 音频综合 * 0.6（可配置权重）

def detect_contradictions(fused: list[dict]) -> list[dict]:
    """检测音视频矛盾点"""
    # 视觉好但音频差 / 音频好但视觉差

def generate_sparklines(fused: list[dict]) -> dict:
    """生成趋势 sparkline 数据"""
```

---

### 阶段三：五维评分 + 画像生成（2-3天）

**目标**：重构评分维度，加入视觉维度，生成能力画像

#### 3.1 重新定义五维评分体系

| 维度 | 子项 | 数据来源 | 权重 |
|------|------|----------|------|
| **内容质量** | 技术深度、创新性、应用价值 | ASR文本 + LLM评分 | 30% |
| **表达力** | 语速控制、流畅度、肢体语言、表情自信 | 音频分析 + 视频分析 | 25% |
| **逻辑结构** | 内容完整性、论证连贯度、板块衔接 | ASR文本 + LLM评分 | 20% |
| **技术深度** | 技术讲解专业度、代码/架构理解 | ASR文本 + LLM评分 | 15% |
| **团队协作** | 分工清晰度、交接自然度、配合默契 | ASR文本 + 视频（交接时段） | 10% |

**实现**：改造 `llm_scoring_service.py` 的评分 prompt，纳入视觉数据

#### 3.2 能力画像生成（`character_profile_service.py`）

**画像是独立模块，与五维评分并列，不是附属品。**

画像输出结构：
```
1. 路演整体节奏分析（热身期/高峰期/疲劳期）
2. 团队分工推断（详细版，见下方）
3. 视觉表现画像（肢体/表情/眼神的趋势和特征）
4. 语音表达画像（语速/停顿/填充词的趋势和特征）
5. 音视频关联洞察（一致点+矛盾点）
6. 个人能力描述（表达力/逻辑力/技术深度/应变力/协作力，用描述性语言，不打分）
7. 关键洞察（3-5条，有证据支撑）
8. 改进建议（针对性、可操作）
```

**团队分工推断（详细版）**：
```python
def infer_team_roles(
    fused_timeline: list[dict],
    asr_segments: list[dict],
    video_frames: list[dict]
) -> list[TeamMember]:
    """
    推断每个选手的：
    - 选手编号/代号（选手A/B/C/D）
    - 负责时间段（start_min ~ end_min）
    - 角色定位（项目经理/开发/测试/演示）
    - 个人表现特征：
      - 语速特点（偏快/稳定/偏慢）
      - 肢体风格（活跃/稳重/拘谨）
      - 表情特征（自信/紧张/平淡）
      - 眼神习惯（看观众/看屏幕/交替）
      - 专业领域（从转录内容推断）
    - 个人强项和弱项
    - 个人改进建议
    """
```

**关键**：画像中的个人能力描述要引用五维评分的数据作为佐证，
五维评分中的维度得分也要在画像中有对应的定性解读。
两者互相印证但独立展示。

#### 3.3 五维评分与画像的关系

```
┌─────────────────────────────────────────────┐
│                完整评估报告                    │
├──────────────────────┬──────────────────────┤
│    五维评分（量化）    │    能力画像（定性）     │
│                      │                      │
│  ┌────────────────┐  │  ┌────────────────┐  │
│  │ 内容质量 30%   │  │  │ 路演节奏分析   │  │
│  │ 表达力   25%   │  │  │ 团队分工推断   │  │
│  │ 逻辑结构 20%   │  │  │ 个人能力描述   │  │
│  │ 技术深度 15%   │  │  │ 关键洞察      │  │
│  │ 团队协作 10%   │  │  │ 改进建议      │  │
│  └────────────────┘  │  └────────────────┘  │
│  ↑ 含音视频实测数据    │  ↑ 引用评分数据佐证    │
└──────────────────────┴──────────────────────┘
```

五维评分的每个维度都要体现音视频分析的贡献：
- **内容质量**：ASR文本 → LLM分析技术深度/创新性/应用价值（主要来源）
- **表达力**：音频(语速/停顿/填充词评分) + 视频(肢体语言/表情自信评分) → 加权合成
- **逻辑结构**：ASR文本 → LLM分析板块衔接/论证完整度（主要来源）
- **技术深度**：ASR文本 → LLM分析代码/架构/安全等专业内容（主要来源）
- **团队协作**：ASR文本(交接口令) + 视频(交接时段的表现变化) → 综合评估

---

### 阶段四：报告升级 + 前端展示（2-3天）

**目标**：生成带音视频融合数据的 PDF 报告，前端展示时间序列

#### 4.1 PDF 报告升级（改造 `report_service.py`）

新增内容板块：
- 📊 音视频融合时间线表格（每2分钟一行）
- 📈 趋势 sparkline 图（肢体语言/表情/语速/融合评分）
- ⚠️ 音视频矛盾点高亮
- 🎯 能力画像描述性段落
- 🔍 关键洞察 + 改进建议

#### 4.2 前端：ScoreResult.vue 升级

新增 UI 组件：
```
ScoreResult.vue
├── 五维雷达图（升级版，含视觉子项）
├── 时间线热力图（色块表示每窗口的融合评分）
├── 音视频对比折线图（Chart.js / ECharts）
├── 矛盾点高亮卡片
├── 能力画像描述区
├── 关键洞察列表
└── 改进建议列表
```

#### 4.3 前端：视频片段证据回放

在报告中嵌入可点击的时间节点，跳转到对应的视频片段：
```
[10min] 融合分最低 5.1 → [点击播放] → 跳转到视频 10:00-12:00
```

---

## 📁 文件变更清单

| 阶段 | 文件 | 操作 |
|------|------|------|
| 一 | `frontend/user/src/components/AudioRecorder.vue` | 🔄 改造：增加视频录制 |
| 一 | `frontend/user/src/views/MeetingRoom.vue` | 🔄 改造：传递 video track |
| 一 | `ai-scoring/app/routers/scoring_router.py` | 🔄 改造：支持视频上传 |
| 一 | `ai-scoring/app/services/audio_service.py` | 🔄 改造：视频→音频提取 |
| 二 | `ai-scoring/app/services/video_analysis_service.py` | 🆕 新增 |
| 二 | `ai-scoring/app/services/fusion_service.py` | 🆕 新增 |
| 二 | `ai-scoring/app/config.py` | 🔄 改造：增加视频配置 |
| 三 | `ai-scoring/app/services/llm_scoring_service.py` | 🔄 改造：五维评分重构 |
| 三 | `ai-scoring/app/services/character_profile_service.py` | 🆕 新增 |
| 三 | `ai-scoring/app/services/pipeline_service.py` | 🔄 改造：编排音视频流程 |
| 四 | `ai-scoring/app/services/report_service.py` | 🔄 改造：融合报告 |
| 四 | `frontend/user/src/views/ScoreResult.vue` | 🔄 改造：融合展示 |

---

## ⏱ 时间估算

| 阶段 | 内容 | 时间 |
|------|------|------|
| 一 | 视频录制 | 1-2天 |
| 二 | 视频分析 + 融合 | 2-3天 |
| 三 | 五维评分 + 画像 | 2-3天 |
| 四 | 报告 + 前端 | 2-3天 |
| **合计** | | **7-11天** |

---

## 🔑 关键技术决策

1. **视频来源**：屏幕共享（含摄像头画中画）> 纯摄像头。路演场景下屏幕共享能同时捕捉演讲者+PPT内容
2. **分析模型**：Qwen3-VL-Flash（快速/便宜）为主，Qwen3.5-Plus（精确）为辅
3. **融合权重**：音频 60% + 视觉 40%（视觉信号不如音频精确，降权处理）
4. **评分策略**：证据式 > 分数式。给评委提供数据和画像描述，而不是替他们打分
5. **说话人分离**：暂不做，先做团队整体画像。等验证价值后再投入
